version: 2

models:
  - name: stg_google_analytics_events
    description: "This tables stages the Google Analytics 4 raw event data source."
    columns:
      - name: user_pseudo_id
        tests:
          - not_null
  - name: stg_google_analytics_traffic
    description: "This table stages the Google Analytics 4 traffic related events."
    columns:
    - name: user_pseudo_id
      tests:
        - not_null
